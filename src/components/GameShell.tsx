import React from 'react';
export function GameMenu({hasSave,onNew,onContinue,onLoad,onSettings,onCodex,onCredits}:{hasSave:boolean;onNew:()=>void;onContinue:()=>void;onLoad:()=>void;onSettings:()=>void;onCodex:()=>void;onCredits:()=>void}){return <main className="game-menu" aria-labelledby="game-title"><p className="eyebrow">THE SUNKEN RELIQUARY · CHAPTER ONE</p><h1 id="game-title">Aether &amp; Iron</h1><p className="menu-subtitle">A tactical expedition of steel, sorcery, and consequence.</p><div className="menu-actions">{hasSave&&<button className="primary-button" onClick={onContinue}>Continue Expedition</button>}<button className="primary-button" onClick={onNew}>New Expedition</button><button onClick={onLoad}>Load Game</button><button onClick={onSettings}>Settings</button><div className="menu-secondary"><button onClick={onCodex}>Codex</button><button onClick={onCredits}>Credits</button></div></div></main>}
export function SimplePanel({title,onBack,children}:{title:string;onBack:()=>void;children:React.ReactNode}){return <main className="simple-panel" aria-labelledby="panel-title"><button className="subtle-button" onClick={onBack}>← Main menu</button><h1 id="panel-title">{title}</h1><div className="panel-content">{children}</div></main>}
